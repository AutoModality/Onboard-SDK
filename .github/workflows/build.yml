name: Story Development
on: 
  push:
    branches-ignore:
      - master
jobs:
  make:
    runs-on: ubuntu-16.04
    name: Make
    steps:
    - name: Checkout
      uses: actions/checkout@v1      
    - name: Make
      uses: docker://docker.cloudsmith.io/automodality/trial/amros-base:latest      
    - run:  |
        mkdir build
        cd build
        cmake .. -DADVANCED_SENSING:BOOL=ON
        make djiosdk-core

